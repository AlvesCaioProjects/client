services:
  client-api:
    build: .
    container_name: client_api
    ports:
      - "8081:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    networks:
      - bank-service

networks:
  bank-service:
    external: true